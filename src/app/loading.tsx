const Loading = () => {
  return (
    <div className="min-h-screen w-full flex items-center justify-center opacity-25">
      <span className="text-3xl font-bold">Loading...</span>;
    </div>
  );
};

export default Loading;
